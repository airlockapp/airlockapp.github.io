---
import DocsLayout from "../../layouts/docs.astro";
import { Code } from "astro:components";

// Code snippets as strings so Astro doesn't parse ${...} as JS expressions

// ── .NET ──
const dotnetInstall = `dotnet add package Airlock.Gateway.Sdk`;
const dotnetAuthInitClients = `// Initialize the gateway client with app credentials
var gwHttp = new HttpClient
{
    BaseAddress = new Uri(gatewayUrl.TrimEnd('/')),
    Timeout = TimeSpan.FromSeconds(120)
};
gwHttp.DefaultRequestHeaders.Add("X-Client-Id", clientId);
gwHttp.DefaultRequestHeaders.Add("X-Client-Secret", clientSecret);

var gwClient = new AirlockGatewayClient(gwHttp);`;
const dotnetAuthSetBearer = `// After user sign-in (Device Auth Grant):
gwClient.SetBearerToken(accessToken);`;
const dotnetQuickStart = `using Airlock.Gateway.Sdk;
using Airlock.Gateway.Sdk.Models;

var httpClient = new HttpClient
{
    BaseAddress = new Uri("https://igw.airlocks.io")
};
httpClient.DefaultRequestHeaders.Authorization =
    new System.Net.Http.Headers.AuthenticationHeaderValue("Bearer", "your-token");

var client = new AirlockGatewayClient(httpClient);

// Submit an artifact for approval
var requestId = await client.SubmitArtifactAsync(new ArtifactSubmitRequest
{
    EnforcerId = "my-enforcer",
    ArtifactType = "command.review",
    ArtifactHash = "sha256-hash",
    Ciphertext = new EncryptedPayload
    {
        Alg = "aes-256-gcm",
        Data = "base64-encrypted-content",
        Nonce = "nonce",
        Tag = "tag"
    },
    Metadata = new Dictionary<string, string>
    {
        ["routingToken"] = "rt-abc"
    }
});

// Wait for a decision (long-poll)
var decision = await client.WaitForDecisionAsync(requestId, timeoutSeconds: 30);
if (decision?.Body?.IsApproved == true)
{
    Console.WriteLine($"Approved: {decision.Body.Reason}");
}`;

// ── Python ──
const pythonInstall = `pip install airlock-gateway`;
const pythonAuthInitClients = `# Initialize the gateway client with app credentials (headers + base URL)
gw_client = AirlockGatewayClient(
    base_url=gateway_url,
    client_id=client_id,
    client_secret=client_secret,
    http_client=gw_http,
)`;
const pythonAuthSetBearer = `# After user sign-in (Device Auth Grant):
gw_client.set_bearer_token(access_token)`;
const pythonQuickStart = `import asyncio
from airlock_gateway import (
    AirlockGatewayClient,
    ArtifactSubmitRequest,
    EncryptedPayload,
)

async def main():
    async with AirlockGatewayClient(
        "https://igw.airlocks.io", token="your-token"
    ) as client:
        # Submit an artifact for approval
        request_id = await client.submit_artifact(
            ArtifactSubmitRequest(
                enforcer_id="my-enforcer",
                artifact_hash="sha256-hash",
                ciphertext=EncryptedPayload(
                    alg="aes-256-gcm",
                    data="base64-encrypted-content",
                    nonce="nonce",
                    tag="tag",
                ),
                metadata={"routingToken": "rt-abc"},
            )
        )

        # Wait for a decision (long-poll)
        decision = await client.wait_for_decision(request_id, timeout_seconds=30)
        if decision and decision.body and decision.body.is_approved:
            print(f"Approved: {decision.body.reason}")

asyncio.run(main())`;

// ── TypeScript ──
const tsInstall = `npm install @airlockapp/gateway-sdk`;
const tsAuthInitClients = `import { AirlockGatewayClient } from "@airlockapp/gateway-sdk";

const gwClient = new AirlockGatewayClient({
  baseUrl: gatewayUrl,
  clientId: clientId,
  clientSecret: clientSecret,
});`;
const tsAuthSetBearer = `// After user sign-in (Device Auth Grant):
gwClient.setBearerToken(accessToken);`;
const tsQuickStart = `import { AirlockGatewayClient } from "@airlockapp/gateway-sdk";

const client = new AirlockGatewayClient({
  baseUrl: "https://igw.airlocks.io",
  token: "your-token",
});

// Submit an artifact for approval
const requestId = await client.submitArtifact({
  enforcerId: "my-enforcer",
  artifactHash: "sha256-hash",
  ciphertext: {
    alg: "aes-256-gcm",
    data: "base64-encrypted-content",
    nonce: "nonce",
    tag: "tag",
  },
  metadata: { routingToken: "rt-abc" },
});

// Wait for a decision (long-poll)
const decision = await client.waitForDecision(requestId, 30);
if (decision?.body?.decision === "approve") {
  console.log(\`Approved: \${decision.body.reason}\`);
}`;

// ── Go ──
const goInstall = `go get github.com/airlockapp/gateway-clients/src/go/airlock`;
const goAuthInitClients = `// Initialize the gateway client with app credentials
gwClient := airlock.NewClientWithCredentials(
    gatewayUrl,
    clientId,
    clientSecret,
)`;
const goAuthSetBearer = `// After user sign-in (Device Auth Grant):
gwClient.SetBearerToken(accessToken)`;
const goQuickStart = `package main

import (
    "fmt"
    airlock "github.com/airlockapp/gateway-clients/src/go/airlock"
)

func main() {
    client := airlock.NewClient("https://igw.airlocks.io", "your-token")

    // Submit an artifact for approval
    requestID, err := client.SubmitArtifact(airlock.ArtifactSubmitRequest{
        EnforcerID:   "my-enforcer",
        ArtifactHash: "sha256-hash",
        Ciphertext: airlock.EncryptedPayload{
            Alg:   "aes-256-gcm",
            Data:  "base64-encrypted-content",
            Nonce: "nonce",
            Tag:   "tag",
        },
        Metadata: map[string]string{"routingToken": "rt-abc"},
    })
    if err != nil {
        panic(err)
    }

    // Wait for a decision (long-poll)
    decision, err := client.WaitForDecision(requestID, 30)
    if err != nil {
        panic(err)
    }
    if decision != nil && decision.Body.IsApproved() {
        fmt.Printf("Approved: %s\\n", decision.Body.Reason)
    }
}`;

// ── Rust ──
const rustInstall = `[dependencies]
airlock-gateway = "0.1"`;
const rustAuthBearer = `use airlock_gateway::*;

let client = AirlockGatewayClient::new(
    "https://igw.airlocks.io",
    Some("your-token"),
);`;
const rustAuthCredentials = `use airlock_gateway::*;

let client = AirlockGatewayClient::with_credentials(
    "https://igw.airlocks.io",
    "your-client-id",
    "your-client-secret",
);`;
const rustAuthDual = `// After user sign-in (Device Auth Grant):
client.set_bearer_token(Some(access_token));`;
const rustQuickStart = `use airlock_gateway::*;

#[tokio::main]
async fn main() -> Result<(), GatewayError> {
    let client = AirlockGatewayClient::new(
        "https://igw.airlocks.io",
        Some("your-token"),
    );

    // Submit an artifact for approval
    let request_id = client.submit_artifact(ArtifactSubmitRequest {
        enforcer_id: "my-enforcer".into(),
        artifact_hash: "sha256-hash".into(),
        ciphertext: EncryptedPayload {
            alg: "aes-256-gcm".into(),
            data: "base64-encrypted-content".into(),
            nonce: Some("nonce".into()),
            tag: Some("tag".into()),
            aad: None,
        },
        artifact_type: None,
        expires_at: None,
        metadata: None,
        request_id: None,
    }).await?;

    // Wait for a decision (long-poll)
    if let Some(decision) = client.wait_for_decision(&request_id, 30).await? {
        if let Some(body) = &decision.body {
            if body.is_approved() {
                println!("Approved: {:?}", body.reason);
            }
        }
    }

    Ok(())
}`;
---

<DocsLayout title="Airlock Gateway Client SDKs">
    <div class="space-y-8">
        <div>
            <h1 class="text-3xl md:text-4xl font-bold">
                Gateway Client SDKs
            </h1>
            <p class="text-muted-foreground mt-2">
                Multi-language client libraries for the Airlock Integrations Gateway,
                built on the
                <a
                    href="https://harp-protocol.github.io"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-link hover:underline">HARP protocol</a
                >.
            </p>
        </div>

        <div class="flex flex-wrap gap-3 mt-4">
            <a
                href="https://github.com/airlockapp/gateway-clients"
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-border/50 bg-muted/30 text-sm font-medium text-foreground hover:bg-muted/60 transition-colors"
            >
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="w-4 h-4"
                    viewBox="0 0 24 24"
                    fill="currentColor"
                    ><path
                        d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12Z"
                    ></path></svg
                >
                GitHub Repository
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="w-3.5 h-3.5 text-muted-foreground"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    ><path
                        d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"
                    ></path><polyline points="15 3 21 3 21 9"
                    ></polyline><line x1="10" y1="14" x2="21" y2="3"
                    ></line></svg
                >
            </a>
            <a
                href="https://harp-protocol.github.io"
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-border/50 bg-muted/30 text-sm font-medium text-foreground hover:bg-muted/60 transition-colors"
            >
                HARP Protocol
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="w-3.5 h-3.5 text-muted-foreground"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    ><path
                        d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"
                    ></path><polyline points="15 3 21 3 21 9"
                    ></polyline><line x1="10" y1="14" x2="21" y2="3"
                    ></line></svg
                >
            </a>
        </div>

        <!-- Overview -->
        <div class="prose dark:prose-invert max-w-none">
            <div
                class="p-4 rounded-xl border border-border/50 bg-muted/30"
            >
                <p class="text-sm text-muted-foreground">
                    All SDKs cover the <strong class="text-foreground"
                        >enforcer-side</strong
                    > Integrations Gateway API: artifact submission, decision polling,
                    exchange management, pairing, presence heartbeats, policies,
                    and discovery. Authenticated endpoints require a
                    <strong class="text-foreground">Bearer JWT</strong> token
                    issued by the Airlock identity provider.
                </p>
                <p class="text-sm text-muted-foreground mt-2">
                    Default production base URL:
                    <code class="rounded bg-muted px-1">https://igw.airlocks.io</code>
                </p>
                <p class="text-sm text-muted-foreground mt-2">
                    Recommended artifact type for command review flows:
                    <code class="rounded bg-muted px-1">command.review</code>
                </p>
            </div>
        </div>

        <!-- Language jump links -->
        <div
            class="sticky top-4 z-20 -mx-1 px-1 py-2 backdrop-blur supports-[backdrop-filter]:bg-background/70"
        >
            <div
                class="flex flex-wrap items-center gap-2 rounded-xl border border-border/50 bg-muted/30 px-3 py-2"
            >
                <span class="text-xs font-medium text-muted-foreground mr-1"
                    >Jump to:</span
                >
                <a
                    href="#dotnet"
                    class="inline-flex items-center rounded-full border border-border/50 bg-background/40 px-3 py-1 text-xs font-medium text-foreground hover:bg-muted/60 transition-colors"
                    >.NET</a
                >
                <a
                    href="#python"
                    class="inline-flex items-center rounded-full border border-border/50 bg-background/40 px-3 py-1 text-xs font-medium text-foreground hover:bg-muted/60 transition-colors"
                    >Python</a
                >
                <a
                    href="#typescript"
                    class="inline-flex items-center rounded-full border border-border/50 bg-background/40 px-3 py-1 text-xs font-medium text-foreground hover:bg-muted/60 transition-colors"
                    >TypeScript</a
                >
                <a
                    href="#go"
                    class="inline-flex items-center rounded-full border border-border/50 bg-background/40 px-3 py-1 text-xs font-medium text-foreground hover:bg-muted/60 transition-colors"
                    >Go</a
                >
                <a
                    href="#rust"
                    class="inline-flex items-center rounded-full border border-border/50 bg-background/40 px-3 py-1 text-xs font-medium text-foreground hover:bg-muted/60 transition-colors"
                    >Rust</a
                >
            </div>
        </div>

        <!-- API Surface -->
        <div class="max-w-none">
            <h2 class="text-xl font-semibold mt-8 mb-4 flex items-center gap-2">
                <span class="w-1.5 h-6 rounded-full bg-emerald-500"></span>
                Gateway API Surface
            </h2>
            <table class="w-full text-sm border-collapse">
                <thead>
                    <tr class="border-b border-border/50">
                        <th class="text-left py-2 font-medium w-20">Method</th>
                        <th class="text-left py-2 font-medium">Endpoint</th>
                        <th class="text-left py-2 font-medium">Description</th>
                    </tr>
                </thead>
                <tbody class="text-muted-foreground">
                    <!-- Integrations discovery -->
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-emerald-500/15 text-emerald-500">GET</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/integrations/discovery</code></td
                        ><td class="py-2">Discover IdP configuration (device auth setup)</td></tr
                    >
                    <!-- Discovery -->
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-emerald-500/15 text-emerald-500">GET</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/echo</code></td
                        ><td class="py-2">Gateway discovery and health check</td></tr
                    >
                    <!-- Artifacts -->
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-blue-500/15 text-blue-500">POST</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/artifacts</code></td
                        ><td class="py-2">Submit an artifact for approval</td></tr
                    >
                    <!-- Exchanges -->
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-emerald-500/15 text-emerald-500">GET</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/exchanges/&#123;requestId&#125;</code></td
                        ><td class="py-2">Get exchange status</td></tr
                    >
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-emerald-500/15 text-emerald-500">GET</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/exchanges/&#123;requestId&#125;/wait</code></td
                        ><td class="py-2">Long-poll for a decision</td></tr
                    >
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-blue-500/15 text-blue-500">POST</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/exchanges/&#123;requestId&#125;/withdraw</code></td
                        ><td class="py-2">Withdraw a pending exchange</td></tr
                    >
                    <!-- Pairing -->
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-blue-500/15 text-blue-500">POST</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/pairing/initiate</code></td
                        ><td class="py-2">Start a new pairing session</td></tr
                    >
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-emerald-500/15 text-emerald-500">GET</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/pairing/&#123;nonce&#125;/status</code></td
                        ><td class="py-2">Poll pairing status</td></tr
                    >
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-blue-500/15 text-blue-500">POST</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/pairing/revoke</code></td
                        ><td class="py-2">Revoke an active pairing</td></tr
                    >
                    <!-- Presence -->
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-blue-500/15 text-blue-500">POST</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/presence/heartbeat</code></td
                        ><td class="py-2">Send a presence heartbeat</td></tr
                    >
                    <!-- DND Policies -->
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-emerald-500/15 text-emerald-500">GET</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/policy/dnd/effective</code></td
                        ><td class="py-2">Fetch effective DND policies for an enforcer/workspace/session</td></tr
                    >
                    <!-- Consent -->
                    <tr class="border-b border-border/30"
                        ><td class="py-2"><span class="inline-block rounded px-1.5 py-0.5 text-xs font-semibold bg-emerald-500/15 text-emerald-500">GET</span></td
                        ><td class="py-2"><code class="rounded bg-muted px-1">/v1/consents/check</code></td
                        ><td class="py-2">Check user consent for an enforcer app</td></tr
                    >
                </tbody>
            </table>
        </div>

        <!-- ──────────────────────────────────────────── .NET SDK ──── -->
        <section
            id="dotnet"
            class="prose dark:prose-invert max-w-none pt-8 border-t border-border/30"
        >
            <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
                <span
                    class="inline-flex items-center justify-center w-8 h-8 rounded-lg bg-purple-600/15 text-purple-500 text-xs font-bold"
                    >C#</span
                >
                .NET — <code class="text-base">Airlock.Gateway.Sdk</code>
            </h2>
            <div class="flex flex-wrap gap-2 mb-4">
                <a
                    href="https://www.nuget.org/packages/Airlock.Gateway.Sdk"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    NuGet ↗
                </a>
                <a
                    href="https://github.com/airlockapp/gateway-clients/tree/main/src/dotnet"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    Source ↗
                </a>
            </div>
            <p class="text-muted-foreground text-sm mb-3">
                .NET Standard 2.0 — compatible with .NET Core 2.0+, .NET
                Framework 4.6.1+, and .NET 5+. Depends on
                <code class="rounded bg-muted px-1">System.Text.Json</code>.
            </p>
            <h3 class="text-base font-medium mt-4 mb-2">Installation</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={dotnetInstall} lang="bash" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={dotnetInstall} lang="bash" theme="github-dark" />
            </div>
            <h3 class="text-base font-medium mt-6 mb-2">Quick Start</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={dotnetQuickStart} lang="csharp" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={dotnetQuickStart} lang="csharp" theme="github-dark" />
            </div>

            <h3 class="text-base font-medium mt-6 mb-2">Authentication</h3>
            <p class="text-muted-foreground text-sm mb-3">
                The test enforcer initializes the gateway client with app credentials and then sets a
                user Bearer token after device sign-in.
            </p>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={dotnetAuthInitClients} lang="csharp" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={dotnetAuthInitClients} lang="csharp" theme="github-dark" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden mt-3">
                <Code code={dotnetAuthSetBearer} lang="csharp" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block mt-3">
                <Code code={dotnetAuthSetBearer} lang="csharp" theme="github-dark" />
            </div>
        </section>

        <!-- ──────────────────────────────────────────── Python SDK ──── -->
        <section
            id="python"
            class="prose dark:prose-invert max-w-none pt-8 border-t border-border/30"
        >
            <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
                <span
                    class="inline-flex items-center justify-center w-8 h-8 rounded-lg bg-yellow-500/15 text-yellow-500 text-xs font-bold"
                    >Py</span
                >
                Python — <code class="text-base">airlock-gateway</code>
            </h2>
            <div class="flex flex-wrap gap-2 mb-4">
                <a
                    href="https://pypi.org/project/airlock-gateway/"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    PyPI ↗
                </a>
                <a
                    href="https://github.com/airlockapp/gateway-clients/tree/main/src/python"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    Source ↗
                </a>
            </div>
            <p class="text-muted-foreground text-sm mb-3">
                Async Python 3.9+ client using
                <code class="rounded bg-muted px-1">httpx</code> and
                <code class="rounded bg-muted px-1">pydantic</code>.
            </p>
            <h3 class="text-base font-medium mt-4 mb-2">Installation</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={pythonInstall} lang="bash" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={pythonInstall} lang="bash" theme="github-dark" />
            </div>
            <h3 class="text-base font-medium mt-6 mb-2">Quick Start</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={pythonQuickStart} lang="python" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={pythonQuickStart} lang="python" theme="github-dark" />
            </div>

            <h3 class="text-base font-medium mt-6 mb-2">Authentication</h3>
            <p class="text-muted-foreground text-sm mb-3">
                The Python test enforcer configures a gateway client with credentials (headers) and sets
                the user Bearer token after device sign-in.
            </p>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={pythonAuthInitClients} lang="python" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={pythonAuthInitClients} lang="python" theme="github-dark" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden mt-3">
                <Code code={pythonAuthSetBearer} lang="python" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block mt-3">
                <Code code={pythonAuthSetBearer} lang="python" theme="github-dark" />
            </div>
        </section>

        <!-- ──────────────────────────────────────────── TypeScript SDK ──── -->
        <section
            id="typescript"
            class="prose dark:prose-invert max-w-none pt-8 border-t border-border/30"
        >
            <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
                <span
                    class="inline-flex items-center justify-center w-8 h-8 rounded-lg bg-blue-500/15 text-blue-500 text-xs font-bold"
                    >TS</span
                >
                TypeScript —
                <code class="text-base">@airlockapp/gateway-sdk</code>
            </h2>
            <div class="flex flex-wrap gap-2 mb-4">
                <a
                    href="https://www.npmjs.com/package/@airlockapp/gateway-sdk"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    NPM ↗
                </a>
                <a
                    href="https://github.com/airlockapp/gateway-clients/tree/main/src/typescript"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    Source ↗
                </a>
            </div>
            <p class="text-muted-foreground text-sm mb-3">
                TypeScript-first, zero production dependencies. Node.js 18+
                with built-in <code class="rounded bg-muted px-1">fetch</code>.
            </p>
            <h3 class="text-base font-medium mt-4 mb-2">Installation</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={tsInstall} lang="bash" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={tsInstall} lang="bash" theme="github-dark" />
            </div>
            <h3 class="text-base font-medium mt-6 mb-2">Quick Start</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={tsQuickStart} lang="typescript" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={tsQuickStart} lang="typescript" theme="github-dark" />
            </div>

            <h3 class="text-base font-medium mt-6 mb-2">Authentication</h3>
            <p class="text-muted-foreground text-sm mb-3">
                The TypeScript test enforcer initializes the gateway client with credentials and sets
                the user Bearer token after device sign-in.
            </p>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={tsAuthInitClients} lang="typescript" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={tsAuthInitClients} lang="typescript" theme="github-dark" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden mt-3">
                <Code code={tsAuthSetBearer} lang="typescript" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block mt-3">
                <Code code={tsAuthSetBearer} lang="typescript" theme="github-dark" />
            </div>
        </section>

        <!-- ──────────────────────────────────────────── Go SDK ──── -->
        <section
            id="go"
            class="prose dark:prose-invert max-w-none pt-8 border-t border-border/30"
        >
            <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
                <span
                    class="inline-flex items-center justify-center w-8 h-8 rounded-lg bg-cyan-500/15 text-cyan-500 text-xs font-bold"
                    >Go</span
                >
                Go —
                <code class="text-base">airlock-gateway</code>
            </h2>
            <div class="flex flex-wrap gap-2 mb-4">
                <a
                    href="https://pkg.go.dev/github.com/airlockapp/gateway-clients/src/go"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    pkg.go.dev ↗
                </a>
                <a
                    href="https://github.com/airlockapp/gateway-clients/tree/main/src/go"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    Source ↗
                </a>
            </div>
            <p class="text-muted-foreground text-sm mb-3">
                Go 1.21+ module with idiomatic error handling and context
                support.
            </p>
            <h3 class="text-base font-medium mt-4 mb-2">Installation</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={goInstall} lang="bash" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={goInstall} lang="bash" theme="github-dark" />
            </div>
            <h3 class="text-base font-medium mt-6 mb-2">Quick Start</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={goQuickStart} lang="go" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={goQuickStart} lang="go" theme="github-dark" />
            </div>

            <h3 class="text-base font-medium mt-6 mb-2">Authentication</h3>
            <p class="text-muted-foreground text-sm mb-3">
                The Go test enforcer creates a credentialed client and sets the user Bearer token after
                device sign-in.
            </p>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={goAuthInitClients} lang="go" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={goAuthInitClients} lang="go" theme="github-dark" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden mt-3">
                <Code code={goAuthSetBearer} lang="go" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block mt-3">
                <Code code={goAuthSetBearer} lang="go" theme="github-dark" />
            </div>
        </section>

        <!-- ──────────────────────────────────────────── Rust SDK ──── -->
        <section
            id="rust"
            class="prose dark:prose-invert max-w-none pt-8 border-t border-border/30"
        >
            <h2 class="text-xl font-semibold mb-4 flex items-center gap-2">
                <span
                    class="inline-flex items-center justify-center w-8 h-8 rounded-lg bg-orange-500/15 text-orange-500 text-xs font-bold"
                    >Rs</span
                >
                Rust — <code class="text-base">airlock-gateway</code>
            </h2>
            <div class="flex flex-wrap gap-2 mb-4">
                <a
                    href="https://crates.io/crates/airlock-gateway"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    crates.io ↗
                </a>
                <a
                    href="https://github.com/airlockapp/gateway-clients/tree/main/src/rust"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full border border-border/50 bg-muted/30 text-xs font-medium hover:bg-muted/60 transition-colors"
                >
                    Source ↗
                </a>
            </div>
            <p class="text-muted-foreground text-sm mb-3">
                Async Rust client using
                <code class="rounded bg-muted px-1">reqwest</code>,
                <code class="rounded bg-muted px-1">serde</code>, and
                <code class="rounded bg-muted px-1">thiserror</code>. Edition 2021.
            </p>
            <h3 class="text-base font-medium mt-4 mb-2">Installation</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={rustInstall} lang="toml" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={rustInstall} lang="toml" theme="github-dark" />
            </div>
            <h3 class="text-base font-medium mt-6 mb-2">Quick Start</h3>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={rustQuickStart} lang="rust" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={rustQuickStart} lang="rust" theme="github-dark" />
            </div>

            <h3 class="text-base font-medium mt-6 mb-2">Authentication</h3>
            <p class="text-muted-foreground text-sm mb-3">
                The Rust SDK supports bearer tokens, app credentials, and setting a user token after sign-in.
                (The Rust test enforcer CLI isn’t present in this repo, so these snippets mirror the SDK README.)
            </p>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden">
                <Code code={rustAuthBearer} lang="rust" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block">
                <Code code={rustAuthBearer} lang="rust" theme="github-dark" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden mt-3">
                <Code code={rustAuthCredentials} lang="rust" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block mt-3">
                <Code code={rustAuthCredentials} lang="rust" theme="github-dark" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden dark:hidden mt-3">
                <Code code={rustAuthDual} lang="rust" theme="github-light" />
            </div>
            <div class="rounded-lg border border-border/50 overflow-hidden hidden dark:block mt-3">
                <Code code={rustAuthDual} lang="rust" theme="github-dark" />
            </div>
        </section>

        <!-- Navigation -->
        <div class="mt-8 flex gap-4">
            <a href="/docs" class="text-sm text-link hover:underline"
                >← All Docs</a
            >
            <a href="/" class="text-sm text-muted-foreground hover:underline"
                >Back to Airlock</a
            >
        </div>
    </div>
</DocsLayout>
